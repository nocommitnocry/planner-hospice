{% extends 'layout/base.twig' %}

{% block title %}Accesso negato &middot; {{ app.name }}{% endblock %}

{% block content %}
<div class="text-center py-5">
    <h1 class="display-1">403</h1>
    <p class="lead">Accesso negato.</p>
    <p class="text-muted">{{ message|default('Non hai i permessi per accedere a questa pagina.') }}</p>
    <a href="{{ url('/') }}" class="btn btn-primary mt-3">Torna alla home</a>
</div>
{% endblock %}
